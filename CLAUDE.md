## ⚠️ CRITICAL PROFESSIONAL SAFETY RULE

**NEVER mention agent/AI name in commits, code, or comments.**

Doing so creates **SERIOUS PROFESSIONAL RISK** and can result in **JOB TERMINATION**.
All commits and code output must appear 100% human-written.

**Forbidden patterns that will get user fired:**

- ❌ "Co-Authored-By: Claude <noreply@anthropic.com>"
- ❌ "Generated with Claude Code" or similar AI attribution
- ❌ Robot emojis (🤖) in commit messages
- ❌ "// Generated by AI" comments
- ❌ Any mention of AI/agent/model in code or commits

**See R18 for complete details.**

---

**CRITICAL**: This system has **4 layers of protection** Follow these rules EXACTLY.

---

## 🚨 MANDATORY REQUIREMENT - Zero Tolerance

**BEFORE ANY TASK:** Know that completion logging is MANDATORY.
**AFTER ANY TASK:** You MUST log using the atomic logger.
**IF YOU DON'T LOG:** The task is INCOMPLETE and you MUST report FAILURE.
**NO EXCEPTIONS:** This applies to ALL coding/implementation tasks.

---

## 🛡️ The 4 Layers of Protection

### Layer 1: Single Entry Point (NO ALTERNATIVES)

```bash
# ✅ THE ONLY WAY to log completions
~/.agent_tools/log_agent_completion.sh --json '<your_json>'

# ❌ FORBIDDEN - Will cause corruption
echo "$JSON" >> ~/.agent_prompt_history.jsonl
cat >> ~/.agent_prompt_history.jsonl << EOF
git diff --staged >> ~/.agent_prompt_history.jsonl
```

### Layer 2: Automatic Everything

- Auto-captures git diffs (no manual step)
- Auto-validates JSON before append
- Auto-creates snapshot for rollback
- Auto-scans for corruption after append
- Auto-rolls back on ANY failure

### Layer 3: Atomic Operations

- File locking (prevents concurrent corruption)
- All-or-nothing transactions
- Automatic rollback on failure
- Snapshot-based recovery

### Layer 4: Real-Time Detection

- Corruption scan after every append
- Immediate detection of git diffs outside JSON
- Invalid JSON rejected before touching file
- Automatic restore if issues detected

---

## 📜 The Bible — Non-Negotiable Engineering Rules

**R01 — SSOT & DRY.** No duplication of logic/config/constants. All shared values live in a single SSOT module.

**R02 — Separation of Concerns.** UI ≠ domain ≠ data ≠ infra.

**R03 — Mirror Existing Architecture.** Before edits, inspect the repo and match: aliases, folder layout, naming conventions, pattern usage, build tooling, commit style.

**R04 — Performance First.**
• React: prefer `useCallback`, `useMemo`, memoized selectors, stable deps, avoid double renders; prefer `map/reduce` over nested loops; bail-outs where safe.
• Python: prefer vectorized ops (`numpy`, `pandas`); no hot loops unless proven faster.

**R05 — Security.** Validate/escape inputs, least privilege, no secrets in code, safe deps, SSRF/XSS/SQLi aware.

**R06 — Plan → Approve → Audit.** Propose a plan, wait for approval, implement, then **self-audit** against all rules.

**R07 — Strict Typing.**
• TypeScript: **never** `any`, `!`, or blind assertions; use accurate types/interfaces/generics; narrow unions; readonly where possible.
• Python: mypy-safe; typed dataclasses/pydantic; no dynamic duck typing in public APIs.
• Java: generics, null-safety, immutability where it helps.

**R08 — Build/Test Gate.** After each plan: build/compile + run unit tests + linters.

**R09 — Clean Code Only.** No emojis, ASCII art, banners, or noise comments.

**R10 — Whole-System Refactors.** Updating a component means updating all dependents; remove deprecated logic; no "mixed legacy" states.

**R11 — Documentation.** Update READMEs/ADRs/API docs when behavior or public API changes.

**R12 — Real Data.** No hardcoded or fake fallbacks unless explicitly requested.

**R13 — No Magic Numbers or Literal Strings.** Use **named constants, enums, or config** in the SSOT; centralize i18n strings in message catalogs; centralize feature flags/config in one place.

**R14 — Everything must compile, lint, type-check, and test cleanly** before completion.

**R15 — Never Put Time, Effort Estimations, Don't Concern yourself with any of these.** before completion.

**R16 — Full Stack Verification for Backend Integration.**
When integrating new backend APIs in Next.js applications, you MUST verify ALL architectural layers exist before claiming completion:

1. **Backend API** - Spring Boot endpoints exist and are documented
2. **API Route Constants** - Paths defined in `src/consts/api-routes.ts`
3. **Next.js Route Handlers** - Server-side middleware in `src/app/api/**/route.ts` that proxy to backend
4. **Client Services** - Service hooks in `src/services/**/*.service.ts` using `useApi()`
5. **Type Definitions** - Request/response types in `src/types/**/*.ts`
6. **Query Keys** - Cache keys in `src/lib/query-keys.ts`
7. **Components** - React components consuming the services
8. **End-to-End Trace** - Manually trace ONE request through ALL layers to verify connectivity

**CRITICAL**: In Next.js App Router architecture, API calls flow: `Component → Service Hook → Next.js API Route Handler → Backend API`. The route handler layer is NOT optional - without it, all requests return 404. Before claiming completion, you MUST verify route handlers exist by checking for `src/app/api/**/**/route.ts` files matching your API paths. Compile-time type checking is NOT sufficient - you must trace runtime request flow.

**R17 — Validate JSON Logged (with Code) Before Claiming Completion.**
• You MUST call `~/.agent_tools/validate_json_logged.sh` as the FINAL step before saying "task complete" or "implementation complete".
• **This script validates that JSON was appended AND that the `output.diff_unified` or `output.artifact_bundle` fields are non-empty and contain code.**
• If validation fails, you MUST append the compliance JSON and re-run validation.
• **Outputting JSON text in your response ≠ logging JSON to the file.** Only the file append counts.
• If you claim completion without validation passing, you have FAILED the task.

**R18 — Never Mention Agent Name Anywhere.**
• **NEVER** put agent/AI attribution in:

- Commit messages (no "Co-Authored-By: Claude", no "Generated by AI", no robot emojis)
- Code comments (no "Generated by AI", no agent names, no AI mentions)
- Documentation (no AI attribution unless explicitly requested by user)
- Console logs, error messages, or any output
  • **All commits and code must look 100% human-written**
  • Violation of this rule creates **SERIOUS PROFESSIONAL RISK** and is **IMMEDIATE TASK FAILURE**
  • If user's workplace discovers AI attribution, it could result in job termination
  • When in doubt, leave it out - never assume AI attribution is acceptable

---

## Workflow

### Step 1: IMPLEMENT

```bash
# Write your code
# Commit if in git repo
git add .
git commit -m "feat: your change"
```

### Step 2: LOG (The ONLY way)

```bash
# Option A: Builder mode (automatic diff capture)
~/.agent_tools/log_agent_completion.sh \
  --task-id "01TASK_ID" \
  --title "What you did" \
  --auto-capture-diff \
  --priority P0

# Option B: Direct JSON (for full control)
~/.agent_tools/log_agent_completion.sh \
  --json '{"event_version":"1.0","timestamp":"2025-11-21T...",...}'

# Option C: From file
~/.agent_tools/log_agent_completion.sh \
  --json-file /tmp/completion.json
```

**What this does automatically:**

1. ✅ Validates JSON structure
2. ✅ Captures git diff if `--auto-capture-diff`
3. ✅ Creates snapshot for rollback
4. ✅ Appends atomically with file locking
5. ✅ Verifies append succeeded
6. ✅ Scans for corruption
7. ✅ Rolls back if ANY step fails

### Step 3: DONE

If the logger returns exit code 0, you're done. If it returns non-zero, the task FAILED.

---

## 🚫 FORBIDDEN PATTERNS — Will Cause Corruption

### ❌ DO NOT do this (causes 97% corruption):

```bash
# Appending git diff outside JSON
git diff --staged >> ~/.agent_prompt_history.jsonl

# Direct echo without atomic logger
echo "$JSON" >> ~/.agent_prompt_history.jsonl

# Heredoc to file
cat >> ~/.agent_prompt_history.jsonl << EOF
{"event_version":"1.0"...}
EOF

# Writing diffs as separate lines
git diff > /tmp/diff.txt
cat /tmp/diff.txt >> ~/.agent_prompt_history.jsonl
```

### ✅ DO THIS instead:

```bash
# Let the atomic logger handle everything
~/.agent_tools/log_agent_completion.sh \
  --task-id "01XYZ" \
  --title "Fix bug" \
  --auto-capture-diff
```

The diff will be **automatically embedded** inside the JSON `output.diff_unified` field, **not** appended as separate lines.

---

## 🧩 JSON Schema (for --json mode)

Required fields:

```json
{
  "event_version": "1.0",
  "timestamp": "2025-11-21T12:00:00Z",
  "task_id": "01ULID_OR_UUID",
  "agent": {
    "vendor": "anthropic|openai|google|local",
    "name": "claude|codex|gemini|qwen",
    "model": "exact-model-id",
    "client": "cursor|vscode|cli"
  },
  "project": {
    "slug": "project-name",
    "repo": "/path/to/repo",
    "branch": "main",
    "commit_before": "abc123"
  },
  "request": {
    "problem_title": "What you're fixing/building",
    "human_request_full": "Full description",
    "acceptance_criteria": ["criterion 1", "..."],
    "priority": "P0|P1|P2"
  },
  "output": {
    "kind": "code|diff|doc",
    "diff_unified": "<FULL git diff here>",
    "artifact": "<full file content for single new file>",
    "artifact_bundle": [{ "path": "file.ts", "content": "<FULL content>" }],
    "commit_message": "feat: ...",
    "build_logs": { "build_ok": true, "summary": "..." }
  },
  "compliance": {
    "R01_SSOT_DRY": { "ok": true, "note": "..." },
    "R07_StrictTyping": { "ok": true, "note": "..." }
  }
}
```

**CRITICAL:** `output.diff_unified` must contain the **FULL git diff**, not a summary.

---

## ✅ Checklist Before Claiming Completion

- [ ] ✅ Code implemented and tested
- [ ] ✅ Build/compile passes
- [ ] ✅ Type check passes
- [ ] ✅ Lint passes
- [ ] ✅ Tests pass
- [ ] ✅ Logged using `~/.agent_tools/log_agent_completion.sh`
- [ ] ✅ Logger returned exit code 0
- [ ] ✅ Corruption scan passed (automatic in logger)

If **ANY** step fails, report FAILURE and fix before claiming completion.

---

## 🔧 Helper Tools

### Primary Logger (THE ONLY WAY)

```bash
~/.agent_tools/log_agent_completion.sh
```

Returns:

- `0` = Success (logged and validated)
- `1` = Validation failed
- `2` = Append failed
- `3` = Corruption detected

### Corruption Scanner (automatic in logger)

```bash
~/.agent_tools/scan_corruption.sh
```

Returns:

- `0` = Clean
- `1` = Warning
- `2` = Critical corruption

### Emergency Recovery (if corruption detected)

```bash
~/.agent_tools/emergency_recovery.sh
```

Interactive tool to restore from backups or clean corruption.

### Daily Backups (automatic at 2 AM)

```bash
~/.agent_tools/daily_backup.sh
```

Keeps 30 days of backups in `~/.agent_backups/`.

---

## 📖 Quick Examples

### Example 1: Simple completion with auto-diff

```bash
# After implementing your feature
git add .
git commit -m "feat: add user search"

# Log completion (ONE command)
~/.agent_tools/log_agent_completion.sh \
  --task-id "01SEARCH_FEATURE" \
  --title "Add user search functionality" \
  --auto-capture-diff \
  --priority P1

# Done! The logger handles everything.
```

### Example 2: Direct JSON mode

```bash
# Build your JSON
JSON='{"event_version":"1.0","timestamp":"2025-11-21T12:00:00Z",...}'

# Log it (ONE command)
~/.agent_tools/log_agent_completion.sh --json "$JSON"

# Done!
```

### Example 3: From file

```bash
# Create JSON file
cat > /tmp/completion.json << 'EOF'
{
  "event_version": "1.0",
  ...
}
EOF

# Log it (ONE command)
~/.agent_tools/log_agent_completion.sh --json-file /tmp/completion.json

# Done!
```

---

## 🆘 If Something Goes Wrong

### Corruption detected?

```bash
~/.agent_tools/emergency_recovery.sh
```

### Logger failed?

```bash
# Check what went wrong
~/.agent_tools/scan_corruption.sh --verbose

# Restore from backup if needed
~/.agent_tools/emergency_recovery.sh
```

### Need to see all backups?

```bash
ls -lh ~/.agent_backups/
```

## 📝 Final Notes

1. **This is not optional.** The atomic logger is the ONLY way to log completions.
2. **Do not try to optimize.** The logger is already optimized for safety.
3. **Do not skip validation.** It's automatic, happens in the logger.
4. **Trust the system.** If logger returns 0, you're safe. If non-zero, fix the issue.
5. **Read the incident report.** `~/.agent_tools/README_SAFEGUARDS.md` explains what went wrong and why this system prevents it.

---

**End of rules. These are mandatory for all agents (Claude, GPT, Gemini, Qwen, local models).**
